export const environment = {
    production: true, // Esto le dice a Angular que es el build final
    // Aquí usamos la URL que ya subiste a Render
    base: 'https://ecobackend-ubbd.onrender.com' 
};